[project]
name = "reusehub-backend"
version = "0.1.0"
description = "ReuseHub Django backend"
requires-python = ">=3.12"

[tool.ruff]
target-version = "py312"
line-length = 100
exclude = [".venv", "migrations"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]
